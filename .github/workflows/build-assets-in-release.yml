name: Build assets in release

on:
  release:
    types: [created]

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: ubuntu-latest
            arch: amd64
          - os: ubuntu-latest
            arch: arm64
          - os: macos-latest
            arch: amd64
          - os: macos-latest
            arch: arm64
          - os: windows-latest
            arch: amd64
          - os: windows-latest
            arch: 386

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      # ============== 模拟构建步骤 (用于测试) ==============
      # 这部分只是为了模拟生成文件，实际使用时请替换为您的真实构建步骤
      - name: Create mock build files for testing
        run: |
          # 确定目标文件夹名称 (与原配置保持一致)
          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            folder_name="ios"
          elif [ "${{ matrix.os }}" = "windows-latest" ]; then
            folder_name="windows"
          else
            folder_name="linux"
          fi
          
          # 创建模拟构建文件
          mkdir -p shared/$folder_name
          
          # 模拟生成的文件 (您可以根据实际需要调整文件名和内容)
          if [ "${{ matrix.os }}" = "windows-latest" ]; then
            echo "Mock Windows binary for ${{ matrix.arch }}" > shared/$folder_name/libopenimsdk-${{ matrix.arch }}.dll
            echo "Mock Windows header" > shared/$folder_name/libopenimsdk.h
          elif [ "${{ matrix.os }}" = "macos-latest" ]; then
            echo "Mock macOS binary for ${{ matrix.arch }}" > shared/$folder_name/libopenimsdk-${{ matrix.arch }}.dylib
            echo "Mock macOS header" > shared/$folder_name/libopenimsdk.h
          else
            echo "Mock Linux binary for ${{ matrix.arch }}" > shared/$folder_name/libopenimsdk-${{ matrix.arch }}.so
            echo "Mock Linux header" > shared/$folder_name/libopenimsdk.h
          fi
          
          echo "Created mock files in shared/$folder_name"
          ls -la shared/$folder_name
      # ============== 模拟构建步骤结束 ==============

      - name: List built files
        run: |
          echo "Checking shared directory:"
          ls -la shared/ || echo "shared directory not found"
          find shared/ -type f || echo "No files found in shared"

      - name: Create archive
        run: |
          # 确定文件夹名称 (与原配置的路径逻辑完全一致)
          if [ "${{ matrix.os }}" = "macos-latest" ]; then
            folder_name="ios"
          elif [ "${{ matrix.os }}" = "windows-latest" ]; then
            folder_name="windows"
          else
            folder_name="linux"
          fi
          
          echo "Looking for folder: shared/$folder_name"
          
          if [ -d "shared/$folder_name" ] && [ "$(find shared/$folder_name -type f | wc -l)" -gt 0 ]; then
            cd shared
            # 使用与原配置相同的命名格式
            if [ "${{ matrix.os }}" = "windows-latest" ]; then
              zip -r ../${{ matrix.os }}-${{ matrix.arch }}-assets.zip $folder_name/
            else
              tar -czf ../${{ matrix.os }}-${{ matrix.arch }}-assets.tar.gz $folder_name/
            fi
            cd ..
            echo "Created archive for $folder_name"
            ls -la ${{ matrix.os }}-${{ matrix.arch }}-assets.*
          else
            echo "No files found in shared/$folder_name"
            exit 1
          fi

      - name: Upload to release
        uses: softprops/action-gh-release@v2
        with:
          files: ${{ matrix.os }}-${{ matrix.arch }}-assets.*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
############

# name: Build assets in release

# on:
#   release:
#     types: [created]

# jobs:
#   build:
#     runs-on: ${{ matrix.os }}
#     strategy:
#       matrix:
#         include:
#           - os: ubuntu-latest
#             arch: amd64
#             build: buildLinux
#           - os: ubuntu-latest
#             arch: arm64
#             build: buildLinux
#           - os: macos-latest
#             arch: amd64
#             build: buildIOS
#           - os: macos-latest
#             arch: arm64
#             build: buildIOS
#           - os: windows-latest
#             arch: amd64
#             build: buildWindows
#           - os: windows-latest
#             arch: 386
#             build: buildWindows

#     steps:
#       - name: Checkout code
#         uses: actions/checkout@v4

#       - name: Setup Go environment
#         uses: actions/setup-go@v5
#         with:
#           go-version: "1.22"

#       - name: Check Go version
#         run: go version

#       - name: Install ARM64 cross-compilation toolchain
#         if: matrix.arch == 'arm64' && matrix.os == 'ubuntu-latest'
#         run: |
#           sudo apt-get update
#           sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
#           echo "CC=aarch64-linux-gnu-gcc" >> $GITHUB_ENV
#           echo "CXX=aarch64-linux-gnu-g++" >> $GITHUB_ENV

#       - name: Install MinGW-w64 using Chocolatey
#         if: runner.os == 'Windows' && matrix.arch == '386'
#         run: |
#           curl -L -o mingw32.7z https://sourceforge.net/projects/mingw-w64/files/Toolchains%20targetting%20Win32/Personal%20Builds/mingw-builds/8.1.0/threads-posix/dwarf/i686-8.1.0-release-posix-dwarf-rt_v6-rev0.7z/download
#           7z x mingw32.7z -oC:/mingw32

#       - name: Setup and build on Windows ${{ matrix.arch }}
#         if: runner.os == 'Windows'
#         run: |
#           ./bootstrap_install_mage.bat
#           $env:GOARCH="${{ matrix.arch }}"
#           if ($env:GOARCH -eq "386") {
#             $env:PATH = "C:/mingw32/bin;$env:PATH"
#             gcc --version
#             $env:CC="gcc -m32"
#             $env:CXX="g++ -m32"
#           }
#           mage ${{ matrix.build }}

#       - name: Setup and build on ${{ matrix.os }} ${{ matrix.arch }}
#         if: runner.os != 'Windows'
#         run: |
#           sudo bash ./bootstrap_install_mage.sh
#           export GOARCH=${{ matrix.arch }}
#           sudo -E mage ${{ matrix.build }}

#       - name: Upload Artifacts
#         uses: actions/upload-artifact@v4
#         with:
#           name: ${{ matrix.os }}-${{ matrix.arch }}-assets
#           path: shared/${{ (matrix.os == 'macos-latest' && 'ios' || (matrix.os == 'windows-latest' && 'windows' || 'linux')) }}/
